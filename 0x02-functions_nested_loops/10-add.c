#include "main.h"
/**
 * add - adding two values and return result
 * @a : values to be added
 * @b : values to be added
 * Return: values resulting from the sum
 */
int add(int a, int b)
{
	return (a + b);
}
